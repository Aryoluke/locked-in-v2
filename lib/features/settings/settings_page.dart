import 'package:flutter/material.dart';
import '../../core/app_state.dart';

class SettingsPage extends StatelessWidget { final AppState state; const SettingsPage({super.key, required this.state});
  @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('SETTINGS')), body: ListView(padding: const EdgeInsets.all(16), children: [const Text('Privacy & control', style: TextStyle(fontSize: 26, fontWeight: FontWeight.w900)), const SizedBox(height: 12), Card(child: Column(children: const [SwitchListTile(value: true, onChanged: null, title: Text('Local-first storage'), subtitle: Text('Writes are kept on this device immediately')), SwitchListTile(value: false, onChanged: null, title: Text('Share body data'), subtitle: Text('Private by default; server sync is not enabled in this build')), ListTile(leading: Icon(Icons.download), title: Text('Export JSON'), subtitle: Text('Export pipeline is a roadmap stub')), ListTile(leading: Icon(Icons.delete_outline), title: Text('Delete account data'), subtitle: Text('Self-hosted account deletion is a roadmap stub'))])), const SizedBox(height: 12), Card(child: ListTile(leading: const Icon(Icons.bolt, color: Color(0xFFD4AF37)), title: const Text('Current level'), subtitle: Text('Level ${state.level} • ${state.levelName} • ${state.xp} XP'))), const SizedBox(height: 12), OutlinedButton(onPressed: () => state.resetDay(), child: const Text('RESET LOCAL DAY'))]));
}
